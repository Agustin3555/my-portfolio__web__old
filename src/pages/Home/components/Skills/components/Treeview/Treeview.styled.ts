import { NOT_FONT_SIZE } from '@/styles'
import styled from 'styled-components'

export const StylizedTreeview = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${NOT_FONT_SIZE['3xs']};
`
